name: Onto to GitHub Pages

on:
  push:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - uses: cgs-earth/onto-to-pages@main
